# Root-level .gitignore (located in the parent folder)

# Ignore node_modules in both client and server
/client/node_modules
/server/node_modules

# Ignore environment files in both client and server
/client/.env
/server/.env

# Ignore VSCode settings
.vscode/